If the errors of a neural network have a probability close to one in a billion, then to confirm this probability a billion test examples are needed. This means that testing well-trained neural networks becomes a very difficult task.