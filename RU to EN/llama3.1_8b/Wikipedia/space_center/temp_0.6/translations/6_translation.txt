If the errors of a neural network have a probability close to one billionth, then it also takes one billion test examples to confirm this probability. It turns out that testing well-trained neural networks becomes a very difficult task.