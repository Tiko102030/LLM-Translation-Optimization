If the probability of errors in a neural network is close to one in a billion, then confirming this probability would require a billion test examples. It turns out that testing well-trained neural networks becomes a very difficult task.