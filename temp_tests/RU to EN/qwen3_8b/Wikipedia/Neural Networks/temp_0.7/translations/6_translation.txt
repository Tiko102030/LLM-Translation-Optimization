If the error of the neural network has a probability close to one billionth, then confirming this probability requires a billion test examples. It turns out that testing well-trained neural networks becomes a very difficult task.