If the errors of the neural network have a probability close to one in a billion, then confirming this probability requires a billion test examples. It turns out that testing well-trained neural networks becomes a very difficult task.